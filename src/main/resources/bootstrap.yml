#spring:
#  application:
#    name: dbconnection #Specify the name of the service so that Spring Cloud Config client knows which service is being looked up
#  profiles:
#    active: default #Specify the default profile the service should run. Profile maps to environment
#  cloud:
#    config:
#      uri: http://localhost:8888 #Specify the location of the Spring Cloud Config server